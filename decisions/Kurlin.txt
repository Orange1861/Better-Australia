country_decisions = {

	diplo_form_kurlin = {
		major = yes
		potential = {
			primary_culture = kulin
			NOT = { exists = KUL }
		}
		provinces_to_highlight = {
			culture = kulin
			OR = {
				NOT = { owned_by = ROOT }
				is_city = no
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			capital_scope = {
				colonial_region = colonial_australia
				is_core = ROOT
			}
		}
		effect = {
			change_tag = KUL
			on_change_tag_effect = yes
			australia_region = {
				limit = {
					AND = {
						NOT = { owned_by = ROOT }
					}
				}
				add_permanent_claim = KUL
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			add_government_reform = aboriginal_kurlin_confederacy
			add_prestige = 25
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
}